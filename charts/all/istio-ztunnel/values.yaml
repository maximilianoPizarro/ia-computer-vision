# Istio version for ZTunnel. Empty = omit spec.version so the Sail operator
# CRD default applies for whatever servicemeshoperator3 CSV is installed
# (safe across 3.1.x / 3.2.x / 3.3.x). Pin only when you know the enum, e.g.
# values-hub-rhpds.yaml sets version: v1.28.8 for RHPDS stable snapshots.
version: ""
namespace: ztunnel
syncWave: "6"
