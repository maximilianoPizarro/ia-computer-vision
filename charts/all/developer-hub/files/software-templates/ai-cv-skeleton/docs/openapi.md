# OpenAPI specification

The NeuroFace Backend API is defined in [openapi.yaml](./openapi.yaml). View the **Definition** tab on the **NeuroFace Backend API** entity in Developer Hub.

Gateway URL: `https://neuroface-spoke-gateway.${{ values.spokeClusterDomain }}/user/${{ values.owner }}/`
