apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neuroface-data
  namespace: ${{ values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
