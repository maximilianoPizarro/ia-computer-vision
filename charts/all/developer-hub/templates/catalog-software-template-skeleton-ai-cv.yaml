{{- $root := "files/software-templates/ai-cv-skeleton/" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: developer-hub-catalog-software-template-skeleton-ai-cv
  namespace: developer-hub
  labels:
    rhdh.redhat.com/ext-config-sync: "true"
  annotations:
    argocd.argoproj.io/sync-wave: "3"
data:
{{- /* Helm .Files.Glob excludes .md files; skeleton uses .mdx which are renamed back to .md at mount */ -}}
{{- range $pattern := list "*" "docs/*" "k8s/*" }}
{{- range $path, $_ := $.Files.Glob (printf "%s%s" $root $pattern) }}
  {{ $path | trimPrefix $root | replace "/" "__" | replace ".mdx" ".md" }}: |
{{ $.Files.Get $path | indent 4 }}
{{- end }}
{{- end }}
