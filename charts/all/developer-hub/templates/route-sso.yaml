{{- /*
  Do NOT create Route/sso here. The upstream rhbk chart already installs an
  Ingress for keycloak.ingress.hostname = sso.<apps-domain>, and OpenShift
  admits that as Route/keycloak-ingress-*. A second Route with the same host
  stays HostAlreadyClaimed (Degraded). Argo CD then fails the developer-hub
  sync and never applies Backstage / KeycloakRealmImport — even with
  ignore-healthcheck. SSO stays reachable via the rhbk Ingress Route;
  Route/keycloak (see route-keycloak.yaml) covers stale OIDC redirects to
  keycloak.<domain>.
*/ -}}
